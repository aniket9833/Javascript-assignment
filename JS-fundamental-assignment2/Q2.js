// In JavaScript, the comma operator , allows you to evaluate multiple expressions within a single statement. It's used to separate multiple expressions where JavaScript expects only one expression. The comma operator evaluates each of its operands (from left to right) and returns the value of the rightmost operand.

const a = 10, b = 5, c = "abc";
console.log(a, b, c);